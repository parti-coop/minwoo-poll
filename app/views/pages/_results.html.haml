%section.results-section
  .container.tags-area
    %button.btn{type:"button"}
      운전하거나 대중교통 이용시
    %button.btn{type:"button"}
      은행 대출, 부동산 계약과 같은 신용관련 업무 안에서
    %button.btn{type:"button"}
      가족관계에서
    %button.btn{type:"button"}
      직장에서
    %button.btn{type:"button"}
      학교에서
    %button.btn{type:"button"}
      커뮤니티 안에서
    %button.btn{type:"button"}
      친밀한 관계 안에서
    %button.btn{type:"button"}
      대중매체에서
    %button.btn{type:"button"}
      기타
  .container
    .results-area
      - @feedbacks.each do |feedback|
        - feedback.find_cases('직장에서').each do |caze|
          .box
            = caze
            .author
              #{feedback.region} 거주, #{feedback.age}
