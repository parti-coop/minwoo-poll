%nav.navbar.navbar-default{style:"margin-bottom:0px"}
  .container-fluid
    .navbar-header
      %a.navbar-brand
        민우회 로고

= render 'about'

= render 'tags'

= render 'results'

= render 'footer'
